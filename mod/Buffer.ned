package node;

//
// TODO auto-generated module
//
simple Buffer like IApp
{
    parameters:
        @display("i=block/buffer");
        int address;
        int bufferSize = default(-1);
    gates:
        input in;
        output out;
}
